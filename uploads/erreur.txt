<!DOCTYPE html> <html lang="fr"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"> <title>Chat QR</title> <!-- Bibliothèque QRcode pour générer le QR --> <script src="[https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js"></script>](https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js">) <!-- Bibliothèque PeerJS pour le peer-to-peer --> <script src="[https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js"></script>](https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js">) <style> *{ box-sizing: border-box; margin: 0; padding: 0; }* *body {* *font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;* *background: #f0f2f5;* *min-height: 100vh;* *display: flex;* *flex-direction: column;* *align-items: center;* *padding: 20px;* *}* *.container {* *max-width: 500px;* *width: 100%;* *background: white;* *border-radius: 20px;* *padding: 25px;* *box-shadow: 0 8px 30px rgba(0,0,0,0.1);* *margin-top: 20px;* *}* *h1 {* *font-size: 1.5rem;* *text-align: center;* *margin-bottom: 20px;* *color: #333;* *}* *#room-section {* *text-align: center;* *}* *#room-section button {* *background: #4CAF50;* *color: white;* *border: none;* *padding: 14px 30px;* *font-size: 1.1rem;* *border-radius: 30px;* *cursor: pointer;* *margin: 10px 0;* *width: 100%;* *transition: background 0.3s;* *}* *#room-section button:hover {* *background: #3e8e41;* *}* *#qr-container {* *margin: 20px auto;* *display: inline-block;* *background: white;* *padding: 10px;* *border: 2px dashed #ccc;* *border-radius: 10px;* *}* *#room-id {* *font-weight: bold;* *color: #555;* *word-break: break-all;* *margin: 10px 0;* *}* *#chat-section {* *display: none;* *}* *#chat-messages {* *background: #e9ecef;* *border-radius: 15px;* *padding: 15px;* *height: 250px;* *overflow-y: auto;* *margin-bottom: 15px;* *border: 1px solid #ddd;* *}* *.message {* *margin-bottom: 10px;* *}* *.message .author {* *font-weight: bold;* *color: #333;* *margin-right: 5px;* *}* *.message .content {* *color: #444;* *}* *.message.local {* *text-align: right;* *}* *.message.local .content {* *background: #007bff;* *color: white;* *display: inline-block;* *padding: 6px 12px;* *border-radius: 18px;* *}* *.message.remote .content {* *background: white;* *display: inline-block;* *padding: 6px 12px;* *border-radius: 18px;* *border: 1px solid #ccc;* *}* *#chat-input-area {* *display: flex;* *gap: 10px;* *}* *#chat-input {* *flex: 1;* *padding: 12px;* *border: 1px solid #ccc;* *border-radius: 25px;* *font-size: 1rem;* *outline: none;* *}* *#send-btn {* *background: #007bff;* *color: white;* *border: none;* *padding: 12px 20px;* *border-radius: 50px;* *cursor: pointer;* *font-size: 1rem;* *transition: background 0.3s;* *}* *#send-btn:hover {* *background: #0056b3;* *}* *#status {* *text-align: center;* *margin-top: 10px;* *font-size: 0.9rem;* *color: #888;* *}* *.info {* *text-align: center;* *color: #666;* *margin: 15px 0;* *}* *#join-message {* *display: none;* *color: green;* *font-weight: bold;* *margin: 5px 0;* *}* *@media (max-width: 400px) {* *.container { padding: 15px; }* *h1 { font-size: 1.2rem; }* *}* *</style>* *</head>* *<body>* *<div class="container" id="app">* *<h1>💬 Chat QR</h1>* *<!-- Section création de salon / affichage QR -->* *<div id="room-section">* *<p class="info">Créez un salon, puis scannez le QR avec l'autre mobile.</p>* *<button id="create-btn">Créer un salon</button>* *<div id="qr-container" style="display:none;"></div>* *<p id="room-id"></p>* *<p id="join-message">✔️ Un participant a rejoint</p>* *</div>* *<!-- Section chat (masquée tant que la connexion n'est pas établie) -->* *<div id="chat-section">* *<div id="chat-messages"></div>* *<div id="chat-input-area">* *<input type="text" id="chat-input" placeholder="Tapez votre message..." autocomplete="off">* *<button id="send-btn">Envoyer</button>* *</div>* *<div id="status">Connexion en cours...</div>* *</div>* *</div>* *<script>* *(function() {* *// ---------- Configuration ----------* *// Utilisation du serveur de signalisation public PeerJS (ne stocke aucun message)* *const PEER_OPTIONS = {* *host: '[0.peerjs.com](http://0.peerjs.com)',* *port: 443,* *secure: true,* *config: { iceServers: [* *{ urls: 'stun:stun.l.google.com:19302' }* *]}* *};* *// ---------- DOM Elements ----------* *const roomSection = document.getElementById('room-section');* *const createBtn = document.getElementById('create-btn');* *const qrContainer = document.getElementById('qr-container');* *const roomIdDisplay = document.getElementById('room-id');* *const joinMsg = document.getElementById('join-message');* *const chatSection = document.getElementById('chat-section');* *const chatMessages = document.getElementById('chat-messages');* *const chatInput = document.getElementById('chat-input');* *const sendBtn = document.getElementById('send-btn');* *const statusDiv = document.getElementById('status');* *// ---------- Variables d'état ----------* *let peer = null; // instance PeerJS* *let conn = null; // connexion data (PeerJS)* *let isCreator = false; // ce mobile est-il le créateur ?* *let roomId = null; // identifiant du salon (chaîne)* *let myPeerId = null; // mon ID PeerJS (affiché dans le QR)* *let remotePeerId = null; // ID du correspondant* *// ---------- Fonctions utilitaires ----------* *function generateUUID() {* *return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {* *const r = Math.random()* 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); }); } // Ajouter un message dans la boîte function addMessage(text, local) { const div = document.createElement('div'); div.className = `message ${local ? 'local' : 'remote'}`; const author = local ? 'Moi' : 'Correspondant'; div.innerHTML = `${author}:${text}`; chatMessages.appendChild(div); chatMessages.scrollTop = chatMessages.scrollHeight; } // Afficher la boîte de chat function showChat() { [roomSection.style](http://roomSection.style).display = 'none'; [chatSection.style](http://chatSection.style).display = 'block'; } // Mettre à jour le statut function setStatus(msg, color = '#888') { statusDiv.textContent = msg; [statusDiv.style](http://statusDiv.style).color = color; } // Initialiser PeerJS côté créateur function initPeerCreator() { const id = 'chatqr-' + generateUUID().substring(0,8); peer = new Peer(id, PEER_OPTIONS); myPeerId = id; peer.on('open', (id) => { console.log('Mon ID (créateur):', id); setStatus('En attente du second mobile...'); // Afficher le QR code avec l'URL complète contenant le roomId const url = window.location.origin + window.location.pathname + '?room=' + encodeURIComponent(roomId) + '&peer=' + encodeURIComponent(id); // Vider et générer le QR qrContainer.innerHTML = ''; new QRCode(qrContainer, { text: url, width: 200, height: 200, correctLevel: QRCode.CorrectLevel.H }); [qrContainer.style](http://qrContainer.style).display = 'block'; roomIdDisplay.textContent = 'Code du salon : ' + roomId; // Écouter les connexions entrantes peer.on('connection', (incomingConn) => { conn = incomingConn; remotePeerId = conn.peer; [joinMsg.style](http://joinMsg.style).display = 'block'; setTimeout(() => [joinMsg.style](http://joinMsg.style).display = 'none', 3000); setupConnection(); }); }); peer.on('error', (err) => { console.error('Erreur PeerJS :', err); setStatus('Erreur : ' + err.message, 'red'); }); } // Initialiser PeerJS côté "join" (scanneur) function initPeerJoiner(creatorId) { peer = new Peer(undefined, PEER_OPTIONS); // ID généré automatiquement peer.on('open', (id) => { console.log('Mon ID (joiner):', id); myPeerId = id; // Connexion au créateur conn = peer.connect(creatorId); setupConnection(); }); peer.on('error', (err) => { console.error('Erreur PeerJS :', err); setStatus('Erreur : ' + err.message, 'red'); }); } // Paramétrer la connexion data une fois établie (commun aux deux rôles) function setupConnection() { if (!conn) return; conn.on('open', () => { console.log('Connexion ouverte avec', conn.peer); setStatus('Connecté !', 'green'); showChat(); // Message de bienvenue addMessage('Chat prêt !', true); // Activer l'envoi chatInput.disabled = false; sendBtn.disabled = false; chatInput.focus(); }); conn.on('data', (data) => { console.log('Données reçues :', data); if (data.type === 'message') { addMessage(data.text, false); } }); conn.on('close', () => { console.log('Connexion fermée'); setStatus('Déconnecté', 'orange'); chatInput.disabled = true; sendBtn.disabled = true; // Tenter de se reconnecter ? }); } // Envoyer un message function sendMessage(text) { if (conn && [conn.open](http://conn.open)) { conn.send({ type: 'message', text: text }); addMessage(text, true); chatInput.value = ''; } else { setStatus('Pas de connexion active', 'red'); } } // ---------- Gestion des événements UI ---------- createBtn.addEventListener('click', () => { isCreator = true; roomId = generateUUID().substring(0, 8); createBtn.disabled = true; createBtn.textContent = 'Salon créé, en attente...'; initPeerCreator(); }); sendBtn.addEventListener('click', () => { const text = chatInput.value.trim(); if (text) sendMessage(text); }); chatInput.addEventListener('keypress', (e) => { if (e.key === 'Enter') { e.preventDefault(); [sendBtn.click](http://sendBtn.click)(); } }); // ---------- Détection de paramètre dans l'URL (pour le joiner) ---------- (function autoJoin() { const params = new URLSearchParams([window.location.search](http://window.location.search)); const roomParam = params.get('room'); const peerParam = params.get('peer'); if (roomParam && peerParam) { // Ce mobile a scanné le QR -> rejoindre isCreator = false; roomId = roomParam; const creatorPeerId = peerParam; // Masquer la création, afficher attente [createBtn.style](http://createBtn.style).display = 'none'; document.querySelector('.info').textContent = 'Connexion au salon...'; setStatus('Connexion en cours...'); initPeerJoiner(creatorPeerId); } else { // Mode création : ne rien faire de plus console.log('Mode création'); } })(); })(); </script> </body> </html>